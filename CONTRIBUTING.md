# 貢獻指南

感謝您協助改善恩典同行。這個專案處理教會牧養與行政資料，請將**資料最小化、最小權限與可追溯性**視為每一項變更的預設原則。

## 開發流程

請從最新主分支建立功能分支。每次提交前執行 `pnpm check` 與 `pnpm test`，並為新的伺服器端權限、資料轉換或資料品質規則補上測試。涉及資料庫的修改必須先更新 `drizzle/schema.ts`，產生並審閱遷移 SQL，確認不會破壞既有資料。

| 類型 | 必要檢核 |
| --- | --- |
| 權限或敏感資料 | 伺服器端拒絕案例、最小權限與遮罩測試。 |
| 資料庫 schema | 遷移檔、升級安全性與關聯索引檢查。 |
| 管理介面 | 空資料、錯誤資料與行動版使用情境。 |
| CSV／匯入匯出 | 欄位標頭、公式注入防護與不可含真實資料。 |

## 禁止提交的內容

請勿提交 `.env`、憑證、API 金鑰、資料庫匯出、真實會友名冊、關懷紀錄、代禱內容、支持資料、螢幕截圖或任何可識別個人的資料。範例、測試與文件也不得使用虛構的顧客評價或捏造的教會個案。

## Pull Request

請在 PR 說明中寫明變更目的、資料模型影響、權限影響與測試結果。如變更會影響既有使用者流程，請附上可重現的驗證步驟；不要以公開資料庫插入測試資料作為驗證方式。
